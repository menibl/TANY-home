# שאלות ל-TANY — אינטגרציית HomeBot (נענו)

תשובות שהתקבלו מ-TANY (מבוססות על קריאת קוד + בדיקה חיה מול השרת), 2026-07-25.

## A. MCP / tany_command

1. **הנתיב `https://tany-helper.com/mcp/home` נכון** — 404 שקיבלנו קודם היה כנראה
   לפני שתיקון בצד TANY (proxy `/mcp` → whatsapp-handler) עלה לשרת. צריך POST,
   `Content-Type: application/json`, בלי `/` בסוף. לבדוק שוב.
2. **הפורמט נכון**: `{"method":"tools/call","params":{"name":"tany_command","arguments":{"text":"..."}}}`.
   השרת לא בודק בפועל את `name` — תמיד מריץ `tany_command`. לא משנה עבורנו.
3. **`tools/list` קיים ומחזיר כלי יחיד** — `tany_command`. אין כלים נוספים, אין
   צורך במנגנון גילוי דינמי.
4. **אין rate limit ייעודי ל-MCP** — אבל קריאות MCP צורכות מהמכסה הרגילה של
   המשתמש (5 הודעות/יום חינם, 50/חודש פרימיום). כשלון מכסה חוזר כהודעת מכסה,
   לא כשגיאת MCP — **צריך טיפול ייעודי בצד שלנו** כדי להציג הודעה ברורה במקום
   "תקלה טכנית".

## B. טוקן פר-משתמש (tanyhome_XXXXX)

5. **נוצר רק דרך וואטסאפ**: "צור מכשיר בית [שם]" → הטוקן מוצג פעם אחת בהודעה.
6. **אין תפוגה בכלל** (אין עמודת expiry ב-`home_devices`). לא צריך refresh.
7. **ביטול**: "בטל מכשיר בית [שם]" (מסמן `enabled=FALSE`, לא מוחק). אין rotate
   ישיר — כדי "להחליף" מבטלים ישן ויוצרים חדש (טוקן חדש, לא אותו ערך).

## C. login למכשיר אישי (נייד/רכב, לא בבית)

8-10. **אין מנגנון login נפרד בכלל** — לא username+password, לא session/JWT.
   המנגנון היחיד הקיים הוא בדיוק אותו טוקן `tanyhome_` מסעיף B (Bearer, ללא
   תפוגה). **החלטה**: למכשיר אישי פשוט יוצרים "מכשיר בית" נוסף בשם ייעודי
   ("צור מכשיר בית הרכב שלי") ומדביקים את הטוקן שמתקבל בדפדפן פעם אחת
   (נשמר ב-localStorage) — לא בונים login אמיתי, פשוט משתמשים באותה תשתית
   טוקנים שכבר קיימת אצלנו.

## D. הודעת וואטסאפ יזומה — עדיפות נמוכה, לא בשימוש כרגע

11. קיים `POST /messages/send` ({phone, to, text}) אבל **בלי שום אימות** בקוד
    שנבדק, וגם אוכף את מכסת ההודעות הרגילה. לא נשתמש בזה כרגע (עדיפות ל-C).
    אם ישתמשו בעתיד — חובה לוודא שהוא לא נגיש מבחוץ בלי הגנה נוספת.
