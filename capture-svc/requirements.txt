numpy==1.26.4
sounddevice==0.4.6
opencv-python-headless==4.9.0.80
requests==2.31.0
websockets==12.0
fastapi==0.110.0
uvicorn[standard]==0.29.0
# Optional physical trigger button (BUTTON_GPIO_PIN). Imports fine on any
# platform; only touches real GPIO hardware when that env var is set.
gpiozero==2.0.1
# gpiozero's default pin factory falls back to the legacy sysfs GPIO
# interface (/sys/class/gpio/export), which is read-only in this
# container on this kernel -- confirmed live: OSError on button init.
# lgpio uses the modern /dev/gpiochip character device instead.
lgpio==0.2.2.0
